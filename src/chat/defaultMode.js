const DEFAULT_CHAT_MODE = Object.freeze({
  name: "default",
  description: "Baseline Cadence Lite companion mode.",
  historyLimit: 20,
  allowTools: false,
  allowMemory: true,
});

module.exports = {
  DEFAULT_CHAT_MODE,
};
